# FavoriteAPI — переменные окружения
  # Скопируйте в .env: cp .env.example .env
  # Заполните значения и запустите: python api.py

  # Обязательно: секрет для Flask-сессий (длинная случайная строка)
  SESSION_SECRET=change-me-to-a-long-random-string-min-32-chars

  # Путь к файлу базы данных SQLite
  DATABASE_PATH=database.db

  # Хост и порт для запуска сервера
  HOST=0.0.0.0
  PORT=5005

  # Username Telegram-бота (без @)
  SAM_BOT_USERNAME=SamGPTrobot

  # Максимальное время ожидания ответа от ИИ (секунды, дефолт 600 = 10 минут)
  REQUEST_TIMEOUT_SECONDS=600

  # Время тишины бота для сбора частей ответа (секунды, дефолт 3.0)
  BOT_QUIET_SECONDS=3.0

  # Serveo туннель — субдомен (по умолчанию favapi → https://favapi.serveo.net)
  SERVEO_NAME=favapi

  # Путь к RSA-ключу для Serveo (стабильный ключ = стабильный URL)
  # SERVEO_KEY_PATH=../serveo_key

  # Путь к файлу логов при завершении (дефолт: ./logi.txt рядом с api.py)
  # LOG_DUMP_PATH=./logi.txt

  # Telegram-уведомления о новом Serveo URL
  # Токен бота для отправки уведомлений
  TG_NOTIFY_TOKEN=
  # ID чатов/каналов через запятую (для каналов: -100XXXXXXXXXX или @username)
  TG_NOTIFY_CHATS=
  